server:
  host: "0.0.0.0"
  port: 8080

logging:
  level: "debug"

cors:
  allowed_origins: ["*"]
  allowed_headers: ["*"]

bitvmx:
  broker_port: 22222

bitcoin:
  network: regtest
  url: http://127.0.0.1:18443
  username: foo
  password: rpcpassword
  wallet: test_wallet
